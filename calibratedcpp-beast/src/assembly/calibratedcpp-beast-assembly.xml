<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>calibratedcpp-beast-assembly</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <files>
        <!-- version.xml at ZIP root -->
        <file>
            <source>${project.basedir}/version.xml</source>
            <outputDirectory>/</outputDirectory>
        </file>
        <!-- Source JAR at top level -->
        <file>
            <source>${project.build.directory}/${project.build.finalName}-sources.jar</source>
            <destName>calibratedcpp-beast.src.jar</destName>
            <outputDirectory>/</outputDirectory>
        </file>
    </files>

    <dependencySets>
        <dependencySet>
            <outputDirectory>/lib</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <scope>runtime</scope>
            <excludes>
                <!-- Provided by the BEAST runtime -->
                <exclude>io.github.compevol:*</exclude>
                <!-- Benchmark-only, not needed at runtime -->
                <exclude>org.openjdk.jmh:*</exclude>
            </excludes>
        </dependencySet>
    </dependencySets>

    <fileSets>
        <fileSet>
            <directory>${project.basedir}/fxtemplates</directory>
            <outputDirectory>/fxtemplates</outputDirectory>
            <includes><include>*.xml</include></includes>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}/examples</directory>
            <outputDirectory>/examples</outputDirectory>
            <!-- Ship example inputs only. MCMC run outputs are large (~3 GB locally),
                 regenerable, and gitignored, so excluding them keeps the assembly fast. -->
            <excludes>
                <exclude>**/*.trees</exclude>
                <exclude>**/*.log</exclude>
                <exclude>**/*.state</exclude>
                <exclude>**/*.ops</exclude>
                <exclude>**/.DS_Store</exclude>
            </excludes>
        </fileSet>
    </fileSets>
</assembly>
